<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mjhr.project.review.dao.ReviewDao">

	<select id="selectAll" resultType="Review">
		SELECT * FROM reviews;
	</select>

	<select id="selectOne" parameterType="String" resultType="Review">
		SELECT * FROM reviews
		WHERE review_id = #{reviewId};
	</select>

	<select id="search" parameterType="SearchCondition"
		resultType="Review">
		SELECT * FROM reviews
		<where>
			<if test="key != null and word != null">
				${key} LIKE CONCAT('%', #{word}, '%')
			</if>
		</where>
		<if test="orderBy != null and orderByDir != null">
			ORDER BY ${orderBy} ${orderByDir}
		</if>
	</select>


	<insert id="insertReview" parameterType="Review">
		INSERT INTO reviews 
		(review_id, user_id, isbn, review_title, review_content)
		VALUES
		(#{reviewId},#{userId},#{isbn},#{reviewTitle},#{reviewContent});
	</insert>

	<delete id="deleteReview" parameterType="String">
		DELETE FROM reviews
		WHERE review_id = #{reviewId};
	</delete>

	<update id="updateReview" parameterType="Review">
		UPDATE reviews
		SET 
		review_title = #{reviewTitle}, review_content = #{reviewContent}
		WHERE review_id = #{reviewId};
	</update>

</mapper>